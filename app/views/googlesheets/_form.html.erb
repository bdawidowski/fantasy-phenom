
<%= form_for(@googlesheet) do |f| %>
  <% if @googlesheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@googlesheet.errors.count, "error") %> prohibited this googlesheet from being saved:</h2>

      <ul>
      <% @googlesheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :sport, "SPORT:" %><br>
    <%= f.text_field :sport, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :link, "GOOGLE LINK:" %><br>
    <%= f.text_field :link, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :date_for, "DATE (MM/DD/YY):" %><br>
    <%= f.text_field :date_for, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary btn-block " %>
  </div>
  
<% end %>
<%= link_to 'BACK', googlesheets_path, class: "btn btn-default btn-block" %>